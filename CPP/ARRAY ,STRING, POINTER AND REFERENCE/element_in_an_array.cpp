#include <bits/stdc++.h>
using namespace std;
int main(){
    int i,n,j,m;
    int arr[i][j];
    cout<<"Enter the value of m and n "<<endl;
    cin>>n>>m;//n is row and m is column
    for(i=0;i<n;i++){
        for(j=0;j<n;j++){
            cin >> arr[i][j];
        }
    }
}